# Variáveis de Ambiente - PostgreSQL DQTimes

# Banco de dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dqtimes

# Usuário de aplicação (usar em produção)
DB_USER=dqtimes_app
DB_PASSWORD=changeme_app_password

# Usuário de migração (usar apenas para migrations)
DB_MIGRATION_USER=dqtimes_migration
DB_MIGRATION_PASSWORD=changeme_migration_password

# Admin PostgreSQL (não usar em produção)
# DB_ADMIN_USER=postgres
# DB_ADMIN_PASSWORD=postgres
